# hosts.ini — Inventory file listing managed hosts
# Stored in GitHub repo: scm-ansible-demo/hosts.ini
# All three group members use localhost so no remote servers are needed

[local]
localhost ansible_connection=local  # manages this machine directly

[all:vars]
ansible_python_interpreter=/usr/bin/python3
